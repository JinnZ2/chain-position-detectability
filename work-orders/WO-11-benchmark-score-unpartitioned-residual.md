# WO-11 -- Benchmark score as an unpartitioned residual

STATUS: IN PROGRESS -- REGISTERED, DELIVERED TEXT NOT LANDED.

This file is a placeholder slot, not the work order. It is the only file
in this directory that is not a verbatim delivered marker, and it says so
here so that nothing downstream reads it as one.

```
registered      2026-09-24
title           benchmark score as an unpartitioned residual
delivered text  NOT IN THE TREE -- operator's paste, not retained in this session's record
run status      NOT STARTED
research dir    none (research/wo-11/ is created when a run order is issued)
```

What is known about the marker, from its title alone:

```
measurand   a benchmark score read as a residual that no partition has been
            applied to -- the score carries every unattributed term at once
tags        OBSERVED / DERIVED / PROPOSED per work-order convention; none
            assigned here, because the delivered text is what assigns them
```

Nothing above is a claim. A title is not a measurand statement, and no
falsifier, scope limit or runnable step is stated here because none was
retained. Filling any of them in from memory would put a marker in the
operator's mouth (`READING_PROTOCOL.md`: a work order is the operator's
sensed shape, delivered verbatim).

## What moves this file

```
delivered text lands   -> this file is REPLACED by the verbatim marker, same
                          filename, and the STATUS line above disappears
run order issued       -> research/wo-11/README.md with a step-status table
neither                -> this slot stands; the index row reads "Registered, text not landed"
```

The audit record for this slot is `AUDIT_NOTES.md` CPD_016.
